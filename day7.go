package main

import (
	"fmt"
	"strings"
)

func advent7A(test string) (int, error) {
	for _, s := range strings.Split(test, "\n") {
		println(fmt.Sprintf("%v", s))
	}
	return 0, nil
}

func advent7B(test string) (int, error) {
	for _, s := range strings.Split(test, "\n") {
		println(fmt.Sprintf("%v", s))
	}
	return 0, nil
}
